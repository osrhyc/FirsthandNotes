export const WEEK_1_ANSWER_DETAILS: Record<string, string> = {
	"d01-event-loop-run-to-completion": `结论：JavaScript 回调遵循运行至完成：一旦开始执行，在当前调用栈清空前，另一个任务回调不能插入。它描述的是主线程上一次执行的原子边界，不等于浏览器只有一个线程。

展开：事件循环只能在栈为空时选择下一项任务；输入事件、定时器和渲染都要等待。若回调持续计算 200ms，这 200ms 内即使后台网络已经完成，结果回调也只能排队，页面会出现输入延迟和掉帧。

例子：大列表同步排序与渲染若占满主线程，可按可观测的时间预算拆到后续任务；纯 CPU 计算还可交给 Worker。拆分后总耗时可能略增，但用户输入获得处理机会。

面试表达：我会先说明“栈不空就不切任务”，再把长任务连接到交互与渲染阻塞，最后给出任务拆分、Worker 和 Performance Trace 验证，并强调不能机械以 50ms 作为流畅保证。`,
	"d01-microtask-checkpoint": `结论：同一轮中先完成当前同步任务，再执行微任务检查点；Promise.then 与 queueMicrotask 都进入微任务队列，通常按入队先后执行，之后才会选择 setTimeout 等后续任务。

展开：微任务不是“速度更快”的异步，而是具有不同调度时机。检查点会持续取出微任务，执行期间新加入的微任务也会在本次检查点继续处理，因此递归追加可能阻止事件循环推进。

例子：脚本依次注册 then、queueMicrotask 和 0ms timer，随后打印同步日志；同步日志先出现，then 与 queueMicrotask 按注册顺序出现，timer 最后。若第一个微任务再追加一个，它排在已有微任务之后。

面试表达：我会限定这是浏览器常见调度模型，按“当前栈—微任务检查点—下一任务”推演，而不说 Promise 永远领先所有异步代码；还会补充微任务饥饿及渲染并非每次检查点后必然发生。`,
	"d01-browser-node-loop-boundary": `结论：不能把浏览器输出题直接套给 Node.js，因为 ECMAScript 只定义 Promise 作业等语言语义，具体事件、I/O 与渲染调度属于宿主；两种宿主的队列和推进规则不同。

展开：浏览器围绕任务源、微任务检查点和渲染机会工作；Node 由 libuv 的 timers、poll、check 等阶段组织 I/O，另有 process.nextTick 特有队列。Node 版本变化还可能调整 timer 的阶段行为。

例子：在 Node 的 I/O 回调中同时安排 setImmediate 与 setTimeout(0)，通常 poll 之后先进入 check；但在主模块中先后受 timer 到期和启动耗时影响，不应把一次本机结果当保证。

面试表达：我会先问运行时和版本，再区分语言层微任务与宿主层阶段，用具体上下文推演；若题目缺少环境，我会明确哪些顺序稳定、哪些只能实验验证，而不是给一个跨环境固定输出。`,
	"d01-output-order-trace": `结论：给定顺序的输出是 1、5、3、4、2。关键不在背答案，而在记录每条代码执行时是立即运行，还是进入微任务队列或 timer 对应的后续任务。

展开：整个 script 是当前任务，日志 1 立即打印；setTimeout 只登记 timer；Promise.then 的 reaction 和 queueMicrotask 依次入微任务队列；日志 5 仍在当前栈中，因此先于所有异步回调。

例子：script 结束、栈清空后，微任务检查点先取出日志 3，再取出日志 4。检查点清空后，事件循环才有机会选择已经到期的 timer 任务，最终打印 2。

面试表达：我会边读代码边画“调用栈、微任务、任务”三栏，给出入队时刻和出队条件；如果 then 内再 queueMicrotask，它追加到当时队尾。若加入 Node API，则重新限定环境，不能沿用浏览器结论。`,
	"d01-promise-constructor-sync": `结论：new Promise 会同步调用 executor；resolve 只锁定或采纳 Promise 的最终状态，不会终止 executor，所以 resolve 后的普通语句仍同步执行；then 回调则在后续微任务中执行。

展开：Promise 的“异步”主要体现为 reaction 的调度，不代表构造器内部全部异步。首次 resolve 或 reject 生效后，后续再次决议通常被忽略；若已决议后 executor 再抛错，也不会把已确定状态改成拒绝。

例子：executor 中打印 A、调用 resolve、再打印 B，构造器外打印 C，并注册 then 打印 D，通常得到 A、B、C、D。D 必须等当前调用栈结束，即使 Promise 已 fulfilled。

面试表达：我会明确区分“executor 同步执行”“状态已确定”和“处理器异步运行”三个时点，再用短代码验证；避免把 resolve 说成 return，也避免说 Promise 构造器会自动把耗时计算移出主线程。`,
	"d01-nested-microtask-order": `结论：第一个微任务执行时新增的微任务会追加到队尾，不会中断当前回调，也不会越过此前已排队的微任务；但通常仍会在下一个普通任务之前执行。

展开：每个微任务本身也运行至完成。微任务检查点以先进先出的方式持续取队首，直到队列为空；因此嵌套层级不代表优先级，决定位置的是实际入队时刻。

例子：队列初始为 A、B；A 执行时追加 C。A 完成后队列为 B、C，顺序就是 A、B、C。若 C 又持续追加自己，检查点可能长期不结束，使 timer、输入和渲染得不到机会。

面试表达：我会画出队列在每一步的状态，强调“追加到尾部”和“本次检查点继续清空”；然后补充递归微任务不是有效的主线程让步，需要切到后续任务或 Worker。`,
	"d01-async-await-split": `结论：调用 async 函数时，函数体从开头同步执行到第一个 await；await 暂停的只是该 async 函数续体，不阻塞整个主线程。await 后的代码通过后续 Promise reaction 恢复。

展开：await 会把表达式转为 Promise 语义并等待其完成；即使值或 Promise 已经 fulfilled，续体也不会在当前调用栈里同步执行。async 函数调用立即返回一个 Promise，后续返回值或异常决定它的状态。

例子：async 函数先打印 A，await Promise.resolve() 后打印 B；调用它后立刻打印 C，顺序为 A、C、B。连续两个 await 形成两个可观察的暂停点，其他微任务可能在其间穿插。

面试表达：我会用“await 前同步、await 后微任务恢复”概括，再说明暂停作用域仅是当前函数；同时指出循环中的 await 可能造成串行，选择串并行要看依赖、限流、取消与失败语义。`,
	"d01-await-loop-serialization": `结论：for...of 内逐项 await 通常让任务按顺序启动并等待；先 map 创建 Promise 再 Promise.all 则通常同时启动，并按输入索引组织结果。二者差异是启动与错误语义，不只是写法。

展开：串行适合前后依赖、顺序副作用或严格限速；全量并发适合相互独立且规模受控的 I/O。Promise.all 任一拒绝会使外层快速拒绝，但不会自动取消其他已启动工作，也不限制并发峰值。

例子：批量请求一千个 URL 时不宜直接全量 Promise.all，可用固定数量 worker 从队列取任务，结果按原索引写回；配合 AbortSignal、超时和明确的失败策略控制资源。

面试表达：我会先问任务是否独立、规模多大、是否要求保序与取消，再在串行、全量并发和有界并发之间选择；强调结果数组保序不等于完成顺序，也不把快速拒绝说成自动终止。`,
	"d01-timer-minimum-delay": `结论：setTimeout(fn, 0) 表示达到最小延迟后，回调才有资格成为可调度任务；它既不是立即调用，也不承诺恰好零毫秒后执行。

展开：当前任务必须先结束，随后还要处理微任务和排在前面的任务。浏览器还可能实施嵌套 timer 最小延迟、后台标签页节流或省电策略；主线程繁忙时实际延迟会进一步增大。

例子：同步计算占用 100ms 时，0ms timer 至少要等计算和微任务完成。视觉动画应优先使用 requestAnimationFrame 对齐重绘节奏，但 rAF 回调过重仍会错过帧。

面试表达：我会说“delay 是下限，不是预约执行时间”，再列出栈、队列与宿主节流三个延迟来源；输出题里只判断相对调度关系，不根据 0ms 推导精确墙钟时间。`,
	"d01-microtask-starvation": `结论：用 queueMicrotask 递归切十万次计算仍可能卡住页面，因为浏览器会持续清空微任务队列；只要每次都补入下一个微任务，后续任务和渲染机会就无法到来。

展开：异步 API 不等于释放主线程。每个计算片仍在主线程执行，微任务之间也通常不会让输入、timer 获得任务级调度。真正让步需要把续作安排到后续任务，或把适合的纯 CPU 工作交给 Worker。

例子：可按短时间预算处理一批数据，然后通过宿主调度机制安排下一批，并用 Performance 面板观察长任务与交互延迟；粒度过细也会增加调度、闭包和消息传输成本。

面试表达：我会先指出问题是“队列级别选错”，不是每片还不够小；方案包括任务拆分、有优先级的调度及 Worker，并用 INP、Long Task 和真实设备 Trace 决定预算。`,
	"d01-rendering-opportunity": `结论：浏览器不会在每个微任务结束后立刻渲染。通常一个任务及其微任务检查点结束后，浏览器再结合刷新节奏、页面可见性和是否需要更新决定是否产生渲染机会。

展开：渲染不是一个可简单等同于 setTimeout 的固定队列项。requestAnimationFrame 回调会在计划的下一次重绘前执行，适合读取帧时间并更新视觉状态；回调仍占主线程预算，做重计算同样会掉帧。

例子：动画中可在 rAF 内集中写 transform；需要布局测量时尽量批量读后批量写，避免读写交错触发同步布局。非视觉的重计算应拆分或移到 Worker，而非全部塞进 rAF。

面试表达：我会回答“微任务清空只是可能渲染的前置条件，不是渲染保证”，再说明 rAF 与帧预算的关系，并用 Performance Trace 检查回调、样式、布局和绘制耗时。`,
	"d01-long-task-decomposition": `结论：拆分 200ms 计算时，单片大小应由交互目标、设备能力和运行时证据决定，不能因为 50ms 常被用于标识长任务，就机械切成 49ms。

展开：主线程必须在片段间真正回到事件循环，输入与渲染才有机会处理。单片预算还要考虑同一帧中的样式、布局和其他脚本；设备越慢，相同工作量耗时越长。纯 CPU 总量很大时应评估 Worker。

例子：先在中低端设备记录 Trace，按数毫秒预算处理批次，每片结束安排后续任务；比较拆分前后的交互延迟、总完成时间和调度开销。总耗时略增但响应改善可能是合理取舍。

面试表达：我会以用户响应目标为起点，说明“长任务阈值是观测分类，不是切片配方”；然后给出动态预算、任务级让步、Worker 三种选择及复测指标。`,
	"d01-node-nexttick-starvation": `结论：process.nextTick 使用 Node 特有队列，在当前操作结束后、事件循环继续进入 libuv 阶段前处理，本身不是 timers、poll 或 check 中的一个阶段。

展开：Node 会优先排空 nextTick 队列。若 nextTick 回调不断再次注册 nextTick，循环就难以推进到 poll 处理 I/O，也不能进入 check 或 timers，表现为 I/O 饥饿和事件循环延迟升高。

例子：递归 nextTick 做批处理，即便每次工作很少，也可能让网络回调长期等待；若意图是让出当前执行再继续，可根据上下文选 setImmediate、有界队列或 Worker，而不是无限 nextTick。

面试表达：我会先把 nextTick 与 libuv 阶段分开，再用“排空后才推进”解释饥饿；同时强调它并非字面上的下一轮事件循环，具体输出仍要注明 Node 版本和注册上下文。`,
	"d01-node-immediate-timeout": `结论：在 Node 主模块同时注册 setTimeout(0) 与 setImmediate，顺序不应视为跨版本、跨机器的保证；在 I/O 回调内注册时，setImmediate 通常会在随后 check 阶段先执行。

展开：主模块进入事件循环前的耗时会影响 timer 是否已达到阈值，Node 版本中的 timer 阶段实现也会改变观察结果。I/O 回调位于 poll 上下文，poll 完成后进入 check，因而 immediate 的相对关系更可解释。

例子：把两者放在 fs.readFile 回调中，通常 immediate 先于零延迟 timer；移到顶层则一次运行可能相反。这里的 timer 延迟仍是阈值，不是精确执行预约。

面试表达：我会先问“顶层还是 I/O 回调、哪个 Node 版本”，再以 poll、check、timers 的阶段关系回答；缺少条件时明确说不保证，绝不把一次控制台输出当规范。`,
	"d01-unhandled-rejection-boundary": `结论：Promise 在后面虽然写了 catch，仍可能先被报告未处理，因为宿主会在约定检查时机判断拒绝是否已有处理器；处理器挂得过晚，报告可能已发生，之后才变为已处理。

展开：可靠错误边界应让调用方 return 或 await 完整链，并在请求、任务或应用顶层立即附加负责的处理逻辑。浏览器 unhandledrejection 与 Node 对未处理拒绝的行为和策略并非完全相同。

例子：先创建 rejected Promise，再用后续 timer 才挂 catch，浏览器可能先触发 unhandledrejection。全局监听适合兜底记录关联信息，不应承担恢复所有业务错误，也不能替代局部回滚。

面试表达：我会从“拒绝发生—处理器挂载—宿主检查”三个时点解释，再给出链路内处理、顶层观测和安全降级的分层方案；不会声称全局监听能捕获所有异步异常。`,
	"practice-d01-event-loop-trace": `结论：先构造一个最小、可复现且明确运行环境的样例，闭卷按调用栈、微任务队列和 timer 任务推演，再运行验证；浏览器与 Node 的结果分开记录。

展开：样例可依次打印 1，注册 setTimeout 打印 2，注册 Promise.then 打印 3，调用 queueMicrotask 打印 4，最后打印 5。预测为 1、5、3、4、2；然后在第一个微任务中再追加微任务，检验其排到已有队尾。每一步记录“立即执行/何时入队/何时可出队”。

例子：测试至少覆盖基础顺序、嵌套微任务、微任务抛错与长同步任务延迟 timer。若增加 process.nextTick 或 setImmediate，另建 Node 用例并记录版本，不把宿主特有 API 混入浏览器结论。实际结果若与预测不同，先检查环境和代码，而非修改理论迎合输出。

面试表达：我会现场画三栏队列，先给顺序，再逐句解释入队；最后说明哪些来自语言 Promise 语义、哪些来自宿主调度，以及递归微任务可能造成饥饿。这样答案可推演、可复核，而非只背数字。`,
	"output-d01-async-incident": `结论：这个输出题应形成一段不超过 90 秒、证据与机制一致的真实案例；没有亲历案例时明确说是方法模板，不能补造用户量、收益或优化百分比。

展开：背景[待填]；用户现象与影响范围[待填]；浏览器或 Node 版本[待填]；第一时间止损动作[待填]；日志、Performance Trace 或请求标识提供的证据[待填]；排除过的网络、服务端或渲染假设[待填]；最终根因[待填]。

例子：修复方案[待填]；为何选择任务拆分、取消旧请求或补齐 Promise 错误边界[待填]；回归测试[待填]；上线观测口径[待填]；真实结果[待填，未知就写未知]；若重做会提前加入的监控与回滚开关[待填]。每个结论要能指回一条证据。

面试表达：口述顺序用“现象—定位—根因—修复—结果—反思”。我会把个人动作与团队动作分开，数据只报有来源的口径；面试官追问时，再展开事件循环、竞态或错误传播机制，以及为何备选方案未采用。`,
	"d02-promise-error-channel": `结论：每次 then 都返回一个新 Promise。回调中 throw 会使新 Promise 拒绝；返回 rejected Promise 会被它采纳；忘记 return 则新 Promise 以 undefined 完成，内部异步工作脱离当前链。

展开：catch 只是 then 的拒绝处理形式，只能处理连接到该链的拒绝。async 函数里的 throw 会让其返回 Promise 拒绝，但 timer 回调内直接抛错不会自动进入外部 Promise 链。

例子：then(() => fetchData()) 可把请求结果与错误继续传递；写成 then(() => { fetchData(); }) 则后续提前执行，fetchData 的失败也可能成为未处理拒绝。

面试表达：我会画出每个 then 产生的新节点，逐一说明 throw、return value、return Promise 和漏 return 的状态，再强调错误边界必须与实际调用链相连，不能说一个 catch 捕获页面所有异步异常。`,
	"d02-closure-lifetime": `结论：闭包让函数持续访问定义时的词法环境，因此能封装私有状态；同一引用关系也可能让已无业务价值的大对象仍保持可达，从而延长其生命周期。

展开：垃圾回收依据从根对象出发的可达性，不判断业务“还用不用”。闭包本身不是泄漏；只有长生命周期的监听器、timer 或缓存通过闭包长期持有 DOM、数据集等，且未按预期释放时才成为问题。

例子：页面卸载后全局事件监听仍引用组件回调，回调又捕获 DOM 和缓存，整条链都可能被保留。清理监听、timer 和订阅后，再用堆快照的 retaining path 验证。

面试表达：我会先讲词法作用域和可达引用，再区分“合法保留”与“非预期泄漏”，最后给出复现、快照、保留路径和清理后的对照证据，而不笼统说所有闭包都泄漏。`,
	"d02-this-prototype": `结论：普通函数的 this 主要由调用形式决定；箭头函数没有自己的 this，而是捕获外层词法 this。属性读取先查对象自身，找不到才沿原型链逐级查到 null。

展开：obj.fn() 中接收者通常是 obj；严格模式下独立调用普通函数的 this 为 undefined。call、apply、bind 可显式指定，但构造调用还涉及 new 的优先规则。原型属性可被同名自身属性遮蔽。

例子：把 obj.fn 赋给变量后直接调用，会丢失原接收者；若 fn 是箭头函数，即便写成 obj.fn() 也不会因此绑定 obj。读取 obj.x 时，自身 x 优先于 prototype.x。

面试表达：我会把“普通函数看调用点、箭头函数看外层”作为主线，再单独讲属性查找；不会说 this 总由定义位置决定，也不会把方法存在对象上与函数被如何调用混为一谈。`,
	"practice-d02-promise-all": `结论：实现关键是把任意 iterable 固定为数组，以原索引保存结果，用 Promise.resolve 吸收普通值或 thenable；空输入完成为空数组，任一拒绝使外层尽快拒绝。

展开：创建外层 Promise，遍历数组并为每项注册处理器。成功时写 result[index] 并增加完成数，只有完成数等于总数才 resolve；失败处理器直接 reject。不能按完成顺序 push，否则会破坏输入顺序。外层拒绝不会取消已启动任务。

例子：测试空数组、普通值与 Promise 混合、后发先到仍保持索引顺序，以及某项拒绝。还应测试 iterable 转换抛错和 thenable；若业务需要取消，另用 AbortSignal 设计，不能冒充 Promise.all 原生语义。

面试表达：我会先写状态变量与不变量，再补边界测试，说明“结果保序”和“执行串行”无关；快速拒绝只决定聚合 Promise 状态，不代表其他副作用停止。`,
	"output-d02-async-refactor": `结论：这份改造决策要说明语义边界如何改变，而不只是把 then 改写成 async/await。所有经历与指标均填写真实来源；没有数据时保留未知。

展开：原调用链[待填]；可复现问题[待填]；错误由哪一层负责[待填]；取消、超时、重试和幂等现状[待填]；方案 A[待填]、方案 B[待填]；基于依赖顺序、并发上限和维护成本的选择理由[待填]。

例子：改造后的启动顺序[待填]；AbortSignal 或请求身份校验[待填]；局部恢复与顶层兜底[待填]；测试矩阵需包含成功、拒绝、超时、取消、乱序和卸载；上线指标及真实结果[待填]，不写无基线百分比。

面试表达：我会按“原问题—约束—备选—选择—错误边界—验证”口述，指出 Promise.all 快速拒绝不自动取消、漏 return 会断链等具体风险，再说明取舍而非只赞美语法可读性。`,
	"d03-generic-constraints": `结论：泛型保留调用点的具体类型，并表达多个位置之间的类型关系；any 会抹去这种关系。extends 约束声明类型参数至少具备什么能力，不是把值转换成目标类型。

展开：identity<T>(value:T):T 能保证输入输出为同一具体类型；K extends keyof T 则保证键来自 T，从而让返回值成为 T[K]。如果类型参数只出现一次且没有关系可保留，泛型可能没有价值。

例子：getProperty<T, K extends keyof T>(obj:T,key:K) 可拒绝不存在的键并精确推导属性类型；面对真正未知的外部输入，应先用 unknown 和运行时校验，而不是虚构泛型安全。

面试表达：我会用“保留关系”区别泛型与 any，再解释约束提供可用操作及错误前移；也会说明推断困难、签名复杂或无关系时不应为了高级感强行引入泛型。`,
	"d03-conditional-infer": `结论：infer 只在条件类型匹配的结构位置声明待推断类型变量，例如提取函数返回值；它是编译期类型推断，不进行任何运行时检查。

展开：T extends (...args:any[]) => infer R ? R : never 可提取 R。当条件左侧是裸类型参数 T 时，联合类型会逐成员计算再合并，称为分发；写成 [T] extends [U] 可关闭分发。

例子：ToArray<string | number> 若为分发条件，会得到 string[] | number[]；元组包裹后判断整体，结果可为 (string | number)[]。重载函数的推断通常取最后的宽泛签名。

面试表达：我会先给一个 ReturnType 风格示例，再用联合类型对比分发与非分发，最后强调 infer 只是类型模式匹配，不能替代 schema 校验或缩窄真实外部数据。`,
	"d03-utility-type-boundary": `结论：DeepPartial 不能简单对所有 object 递归，因为 object 还包括函数、数组、Date 等具有特殊语义的类型；递归边界必须由具体业务数据模型定义。

展开：常见策略先保留原子类型和函数，数组则递归其元素，最后才映射普通对象属性为可选。Date、Map、Set、元组、只读属性及递归深度是否处理，都应成为显式设计决定。

例子：把函数当对象递归会把其成员映射成可选而丢失可调用性；把数组直接映射可能破坏元素和方法语义。API 更新 DTO 往往还有不可更新字段，不一定等于 DeepPartial<Domain>。

面试表达：我会先指出 object 范围过宽，再列原子、函数、数组、普通对象的分支与未覆盖边界；最后强调这只是编译期便利，外部 JSON 仍必须运行时验证。`,
	"practice-d03-utility-types": `结论：五个类型应从映射类型、键约束和条件推断逐步实现，并同时写“应通过”和“应报错”的编译期用例；DeepPartial 需要声明可接受的边界。

展开：MyPartial 遍历 keyof T 并加可选修饰；MyPick<T,K extends keyof T> 只映射 K；MyOmit 可用 MyPick<T, Exclude<keyof T,K>>；MyReturnType 匹配函数并 infer 返回值。DeepPartial 先保留函数和原子类型，数组递归元素，再处理对象。

例子：用 satisfies、类型相等断言或 ts-expect-error 验证必填变可选、Pick 拒绝非法键、Omit 删除目标键、ReturnType 精确推导，以及函数仍可调用、数组元素递归。明确 Date、Map、元组的当前策略。

面试表达：我会边写边解释每个类型的输入约束和输出关系，不抄内置实现；最后说明联合分发、infer 和递归成本，并强调类型测试不能替代 API 数据的运行时校验。`,
	"output-d03-typescript-adr": `结论：ADR 要把不可信外部 DTO 与可信领域模型隔开：TypeScript 负责已验证代码内的静态关系，运行时 schema 或显式解析负责网络、存储等边界数据。

展开：上下文与目标[待填]；外部输入来源和不可信字段[待填]；领域不变量[待填]；候选一直接断言、候选二 schema 校验、候选三适配层[待填]；最终选择、依赖体积、维护与性能代价[待填]。

例子：DTO 到 Domain 的转换规则[待填]；未知字段、缺失字段和版本兼容策略[待填]；失败响应与日志脱敏[待填]；类型正反用例、有效/无效 payload 和兼容性测试[待填]。不把 as 断言当验证。

面试表达：我会先说“编译期类型在运行时被擦除”，再从信任边界解释为何校验，最后给出方案取舍与失败路径；复杂类型只有减少真实错误时才值得引入。`,
	"d04-network-critical-path": `结论：从 URL 到 HTML 首字节，可能经过 URL 解析、缓存或 Service Worker、DNS、连接建立、TLS、HTTP 请求发送、服务端处理与首字节回传；实际关键路径会因复用和缓存而改变。

展开：同源已有连接可跳过 DNS/TCP/TLS，缓存命中甚至无需网络；代理、HTTP 版本和重定向也会增加或重排阶段。因此不能把固定流程说成每个资源都完整重复。

例子：TTFB 高时先看 Navigation Timing：DNS/connection/TLS 是否非零、requestStart 到 responseStart 是否长，再结合服务端计时区分网络距离与后端处理；一次只验证一个假设。

面试表达：我会先给完整候选链，再马上说明跳过条件，排查时用 Timing 分段而非凭感觉；优化也按真实瓶颈选择预连接、缓存、CDN 或服务端治理。`,
	"d04-rendering-pipeline": `结论：DOM/CSSOM 描述结构与规则，样式计算得到最终样式，布局计算几何，绘制生成像素指令，合成组合图层。重排与重绘不是同义词。

展开：修改 width 常使几何失效，从布局继续影响绘制与合成；transform 在元素已适合独立合成时可能只需更新合成，但浏览器决定是否升层，不能保证任何 transform 都零布局、零绘制。

例子：动画优先考虑 transform/opacity，并用 DevTools 检查 Layout、Paint 和图层；过度 will-change 会增加纹理内存和合成管理成本，也可能得不偿失。

面试表达：我会按每阶段“解决什么”回答，再以 width 与 transform 比较失效范围；最终用 Trace 验证而不背性能口诀，并补充图层数量、内存和设备差异边界。`,
	"d04-http-cache": `结论：max-age 管新鲜度，在有效期内可直接复用；ETag 是重新验证的标识，过期后客户端可条件请求，资源未变则服务端返回 304。两者角色不同且可配合。

展开：内容哈希文件名意味着内容变化会产生新 URL，适合长 max-age 和 immutable；HTML 负责引用最新版资源，通常短缓存或要求重新验证。304 省响应体，但仍有网络往返。

例子：app.abc123.js 可缓存一年且 immutable，新发布生成 app.def456.js；index.html 使用 no-cache 并非“不存储”，而是复用前需验证。敏感响应是否缓存还需单独评估。

面试表达：我会用“新鲜期”和“验证器”区分概念，再给哈希静态资源与 HTML 的成对策略；不会把 memory/disk cache 的存储位置与 HTTP 指令混为一谈。`,
	"practice-d04-waterfall": `结论：先以导航主文档为根，沿资源发现和依赖关系标出关键 CSS、阻塞脚本与 LCP 资源，再从排队、连接、等待、下载各阶段提出可验证瓶颈假设。

展开：记录 Navigation 的 TTFB；检查 CSS/JS 是由 HTML 何时发现，是否阻塞解析或渲染；确认真正的 LCP 元素及其资源请求开始时间。连接已复用时不要重复归因 DNS/TLS，瀑布相关性也不能直接证明因果。

例子：实验 A 仅为 LCP 图片增加正确 preload，实验 B 仅延后非关键脚本；在相同网络、缓存和设备条件分别复测请求发现时间、LCP 和主线程任务。若无改善，回退假设继续定位，不同时改多项。

面试表达：我会按“证据—假设—单变量实验—复测”讲关键路径，区分服务器等待、资源发现和主线程执行；只说懒加载、CDN 等通用词不算诊断。`,
	"output-d04-navigation-map": `结论：关键路径图必须基于真实页面证据，把导航到可交互的依赖关系和每个假设的验证指标画清；没有 Trace 的节点标为未知，不提前写成根因。

展开：页面与测试条件[待填]；缓存或 Service Worker[待填]；DNS/连接/TLS[待填]；HTML TTFB[待填]；关键 CSS、JS 和 LCP 资源的发现关系[待填]；解析、长任务、布局、绘制与交互就绪[待填]。

例子：每个节点补“现有证据[待填]、瓶颈假设[待填]、指标[待填]、最小实验[待填]”。优先级按用户影响和依赖阻塞程度排序；复测固定设备、网络、缓存与版本，并记录回归指标。

面试表达：我会先沿主链口述，再指出最大不确定性和首个实验；清楚区分网络耗时与主线程耗时，并说明连接复用、缓存命中会让部分阶段消失。`,
	"d05-vue-dependency-tracking": `结论：Vue 3 通过响应式代理的读取追踪与写入触发，把属性和依赖它的 effect 连接起来；组件渲染本身就是一个响应式 effect，因此状态变化会安排组件更新。

展开：get 时若存在 activeEffect，track 将 target、key 和 effect 存入依赖集合；set 检测值变化后，trigger 找到集合并交给调度器。更新通常会批处理，而不是每次写入都立刻同步重绘。

例子：渲染读取 user.name 后，修改该键会触发关联 effect；若分支从读取 name 切到 age，重新执行前需清理旧依赖，否则之后改 name 仍会造成无效运行。

面试表达：我会用 get/track、set/trigger、scheduler 三步解释，再连接组件渲染 effect 与批量更新；不会说 Vue 3 必须预先递归遍历所有属性才能监听。`,
	"d05-computed-watch": `结论：computed 用于声明可缓存的派生值；watch 监听显式来源并执行副作用；watchEffect 在同步执行期间自动收集它读取的依赖，也用于副作用。三者不是可随意互换的 API。

展开：computed 应尽量纯粹，依赖不变可复用缓存。watch 便于控制来源、拿到新旧值；watchEffect 适合来源与逻辑紧密但依赖不宜手列的场景。异步回调还要清理过期工作。

例子：fullName 由 firstName 和 lastName 得到，应使用 computed，不应 watch 后再存一份；query 变化请求接口可用 watch，并在清理函数中取消旧请求，避免乱序覆盖。

面试表达：我会按“派生值还是副作用、来源显式还是自动”选择，再说明 flush、清理和异步依赖收集边界；避免用 watch 维护可计算的重复事实源。`,
	"d05-vue-render-key": `结论：key 表达新旧节点之间稳定的业务身份，帮助 patch 正确复用或替换组件与 DOM；它的目的不只是消除警告，也不保证一加 key 就更快。

展开：稳定身份可让输入值、焦点和组件局部状态跟随同一实体。使用数组索引时，头部插入或排序会让同一位置代表不同实体；随机 key 每次都变化，则迫使节点卸载重建。

例子：可编辑用户列表应使用 user.id。只有列表静态、不会重排增删、条目无独立状态时，索引 key 的风险才较低；主动改变表单 key 则可用于明确重置整棵子树。

面试表达：我会从“身份匹配与状态归属”解释，给出索引错配和随机重建的具体后果，再补充可接受边界；不把 key 简化为 diff 性能开关。`,
	"practice-d05-mini-reactivity": `结论：最小实现需要正确的依赖图、effect 生命周期清理和 computed 的脏标记；仅做到 get 时收集、set 时执行，还不足以处理分支切换与缓存。

展开：使用 WeakMap<target, Map<key, Set<effect>>>。effect 运行前从旧 deps 中删除自身，设置 activeEffect，结束后恢复；track 建立双向关系；trigger 复制集合后调度以免边遍历边修改。computed 首次读取才求值，依赖变化只标 dirty，并通知订阅它的 effect。

例子：effect 根据 flag 读取 a 或 b；flag 切换后，再改旧分支 a 不应运行。测试 computed 多次读取只计算一次，依赖变化后第一次读取重算；还要避免 effect 自触发死循环，并验证嵌套 effect 的 activeEffect 恢复。

面试表达：我会先画依赖结构和不变量，再实现清理与调度，最后用分支切换和惰性缓存测试证明正确性；同时声明这是教学模型，未覆盖 Vue 的全部代理、集合和调度细节。`,
	"output-d05-vue-state-map": `结论：状态归属图要为每项状态确定唯一事实来源，并让位置服从读写范围和生命周期；可由其他状态稳定推导的值不重复保存，副作用另行治理。

展开：业务场景[待填]。逐项填写状态名[待填]、事实来源[待填]、读写者[待填]、生命周期[待填]、是否需要 URL 可分享或刷新恢复[待填]、是否为服务端缓存[待填]、当前位置与痛点[待填]。

例子：局部展开状态留组件；跨页面筛选若需分享放 URL；服务端数据交由请求缓存层；真正跨组件客户端状态再进 Pinia。可 computed 得到的副本[待填]应删除；watch 副作用的取消、错误与清理策略[待填]。

面试表达：我会按“谁拥有、谁修改、活多久、能否派生”解释迁移，给出收益和迁移风险[待填]；不会把所有状态都塞进全局仓库，也不会为展示便利制造双事实源。`,
	"d06-react-render-commit": `结论：render 阶段调用组件以计算下一棵 UI；commit 阶段才把变更应用到宿主环境，并处理相关 Effect。render 必须纯粹，因为它可能被重复、暂停或放弃。

展开：纯 render 对相同输入应只描述 UI，不修改外部系统。若在 render 发请求、改全局变量或操作 DOM，一旦 React 为调度或开发检查重跑，就会产生重复副作用和不可预测状态。

例子：根据 props 计算 filteredItems 可直接在 render 做；由点击引发的提交放事件处理器；与订阅、媒体或网络等外部系统同步才放 Effect，并实现对称清理。

面试表达：我会用“计算”和“落地”区分两阶段，再以可重复执行解释纯函数要求；Fiber 不只是一棵虚拟 DOM 数据结构，还服务于工作拆分和调度，但不需要夸大并发保证。`,
	"d06-effect-synchronization": `结论：Effect 的依赖由 Effect 代码读取的响应式值决定，不是开发者按期望手工挑选。删依赖只会让闭包继续使用旧值，并不会让真实数据关系消失。

展开：Effect 用于把 React 状态同步到外部系统。若某段逻辑不应随值变化，先判断它是否属于事件处理、能否在 render 派生，或能否把对象与函数移入 Effect；压制 lint 通常隐藏设计问题。

例子：按 query 请求数据就必须依赖 query，并在 cleanup 中取消旧请求；若“发送购买请求”只由点击触发，应放点击处理器，而不是通过某个布尔状态和 Effect 间接触发。

面试表达：我会先问同步的外部系统是什么，再从代码读取推导依赖，最后讲清理和竞态；开发模式重复执行可暴露不对称清理，不应简单以“只想运行一次”关闭检查。`,
	"d06-react-memoization": `结论：useMemo 缓存计算值、useCallback 缓存函数引用、React.memo 尝试跳过 props 未变的子组件渲染；它们有比较、存储和认知成本，不是默认全部开启的开关。

展开：只有计算确实昂贵，或稳定引用能让已 memo 的下游避免有意义的工作时才可能获益。一个每次新建的对象即可击穿浅比较；错误依赖还会造成旧闭包，增加维护风险。

例子：先用 Profiler 找到频繁且昂贵的提交，优先缩小状态范围、拆分组件或移除不必要 Effect；确认瓶颈后再 memo，并以相同交互比较提交次数、耗时和内存。

面试表达：我会把 memo 视为需要证据的优化，说明收益成立的完整链路和失效条件；函数引用稳定只代表身份未变，不代表业务行为天然正确或组件一定不渲染。`,
	"practice-d06-react-effects": `结论：修复目标是让 Effect 的同步关系完整、旧请求无法提交新状态，并删除能在 render 推导的重复状态；只补依赖数组还不够。

展开：query 是真实依赖。每次 Effect 创建 AbortController 或唯一请求标识，cleanup 取消或作废上一请求；提交结果前确认当前身份。catch 区分 AbortError 与真实失败，finally 也需避免旧请求覆盖当前 loading。过滤、排序等派生结果直接计算，确有昂贵证据时再 memo。

例子：快速输入 a、ab、abc，人工让 a 最晚返回，最终只能显示 abc；卸载后请求取消；空 query 清空结果且不发请求；网络失败展示当前请求错误。测试还应覆盖 Strict Mode 的 setup-cleanup-setup 序列。

面试表达：我会先指出缺失依赖造成旧闭包、乱序造成状态覆盖是两个问题，再展示取消和身份校验；最后说明 Effect 负责外部同步，而非复制所有可派生状态。`,
	"output-d06-react-performance": `结论：性能案例卡必须从可复现交互和 Profiler 证据出发，再选择状态边界、组件结构或 memo；若使用预置案例，应明确标注“练习”，不能声称为个人项目。

展开：案例类型（真实或练习）[待填]；触发交互与测试条件[待填]；用户现象[待填]；Profiler 中提交次数、耗时组件和原因[待填]；根因假设[待填]；备选方案及取舍[待填]。

例子：优先尝试的状态下沉、children 组合或 Effect 清理[待填]；若采用 memo，说明稳定 props 链[待填]；相同条件前后数据[待填]；内存、复杂度与可读性代价[待填]；回归指标和撤销条件[待填]。

面试表达：我会按“交互—Profiler 证据—假设—最小改动—复测”讲，不报无来源数字，也不把一次渲染本身称为问题；追问时说明为什么结构改动或 memo 命中具体瓶颈。`,
	"d07-state-identity": `结论：React key 与 Vue key 的共同本质是帮助协调过程识别前后节点是否代表同一实体。身份稳定时可复用实例和局部状态；key 改变则旧实体被移除、新实体被创建。

展开：状态通常与组件在树中的位置和身份关联，而不是跟着 JSX 文本或数组索引自动迁移。key 是协调提示，不会作为普通 prop 自动传给组件；随机 key 会导致无谓卸载重建。

例子：切换不同用户的编辑表单时，把 userId 作为 key 可有意清空草稿；可排序列表应使用业务 id，否则索引变化可能把输入状态错误复用到另一条记录。

面试表达：我会从身份和状态归属回答，不只说“优化 diff”；接着分别给稳定复用、主动重置和错误索引三个场景，并强调使用 key 是语义决策而非无条件性能提升。`,
	"d07-runtime-evidence": `结论：输出顺序或更新时机题应先限定浏览器、Node、框架和版本，再根据机制逐步推演，最后用最小实验或 Trace 验证；记忆只能作为假设。

展开：JavaScript 题画同步栈、微任务和宿主任务；浏览器题补渲染机会；Node 题补 libuv 阶段与特有队列；框架题区分触发、调度、render 与 commit。稳定规则和实现细节要分开。

例子：setImmediate 与 setTimeout 的先后必须说明注册上下文；框架批处理也需给版本和入口。无法运行时，可说“在这些前提下我推演为……，不确定点是……，可用以下最小代码验证”。

面试表达：我会先报结论和前提，再展示队列或阶段变化，最后明确证据与不确定性；一次本机输出只能支持当前环境观察，不能升级成规范保证。`,
	"d07-senior-answer-structure": `结论：高级回答应先直接给结论，再讲最小必要机制，随后说明适用边界与取舍，最后给出验证证据；工程判断来自约束和证据，不来自术语数量。

展开：两分钟内可用“结论—机制—场景—边界—验证”结构。定义只占开头一句，核心篇幅回答为什么、什么时候不成立、如何选择；遇到版本差异要主动限定。

例子：回答 memo 时先说不应默认添加，再解释缓存成本和稳定引用链，给昂贵子组件场景，指出新对象会击穿，最后用 Profiler 前后对照验证。

面试表达：我会先用 20 秒给可独立成立的主答案，面试官追问再展开细节；避免从历史背景长篇铺垫，也避免罗列十个没有优先级的优化点。`,
	"practice-d07-foundation-mock": `结论：本次模拟要在 45 分钟内闭卷完成六道原理题和一道有界并发编码题，评价重点是准确、可推演、有边界、可验证，而不是关键词覆盖数量。

展开：六题各限四分钟，按 Event Loop、闭包、条件类型、渲染、Vue 响应式、React Effect 依次录音或保留原答。编码题定义输入、并发上限、结果顺序、拒绝策略和取消边界；实现 worker 队列并写边界测试。剩余时间回听并逐题打 0–3 分。

例子：并发测试记录任意时刻 active 不超过 limit；用不同延迟验证结果按输入索引；覆盖空输入、limit 非法和某项拒绝。原理题若只有定义无边界，不能给满分。只把低于 2 分且影响高频面试的项放入弱项池。

面试表达：复盘保留“原答—缺失机制—理想答—变体题—复查日”，最多选三个下一动作。对不会的点准确承认并给验证路径，比用模糊术语补满四分钟更可信。`,
	"output-d07-week-one-scorecard": `结论：能力雷达必须由本周原答和代码测试支撑，六维分别评分后只选三个最高优先缺口；它是训练决策工具，不是自我感受表。

展开：JS[0–3 分，原答证据待填]；TS[0–3 分，证据待填]；浏览器[0–3 分，证据待填]；Vue[0–3 分，证据待填]；React[0–3 分，证据待填]；表达[0–3 分，证据待填]。0 为无法答，1 为关键词，2 为机制，3 为边界与验证。

例子：缺口一的失败题[待填]、缺失机制[待填]、下一次变体题[待填]、复查日[待填]；缺口二[待填]；缺口三[待填]。已有强项只进入间隔复习，不因焦虑继续扩大题库；分数变化保留同口径证据。

面试表达：我会说明评分来自哪些录音、原答或测试，不把一次偶然失误泛化成能力不足；下周动作必须具体到“何时用什么变体重新闭卷输出”，并在复查日重新评分。`,
};
