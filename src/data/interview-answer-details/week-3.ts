export const WEEK_3_ANSWER_DETAILS: Record<string, string> = {
	"d15-node-loop-phases": `结论：timers 处理已到期的 setTimeout、setInterval 回调，poll 获取并执行大部分 I/O 回调，必要时等待新的 I/O；check 专门执行 setImmediate 回调。阶段只决定回调获得执行机会的位置，不保证精确时刻。

展开：事件循环每次推进都受当前回调耗时、poll 队列、操作系统调度和 libuv 版本影响。定时器记录的是“至少等待多久”，阈值到了仍要等 JavaScript 线程空闲。不能把整套阶段压缩成浏览器里的一个“宏任务队列”。

例子：在某些 I/O 回调里同时安排 setImmediate 与 setTimeout(0)，通常会先进入接下来的 check 阶段执行 setImmediate；但在顶层代码中二者先后不应被当成稳定契约。

面试表达：我会先讲三阶段职责，再强调“调度位置不等于时间承诺”，最后用 I/O 回调内的例子说明顺序，并主动补一句顶层顺序会受运行环境影响。`,

	"d15-nexttick-microtasks": `结论：process.nextTick 与 Promise 微任务都会在事件循环进入后续阶段前被清空；如果回调不断递归追加同类任务，timer、I/O 和 setImmediate 就可能长期拿不到执行机会，形成事件循环饥饿。

展开：nextTick 是 Node 特有队列，通常比标准 Promise 微任务优先处理；queueMicrotask 则沿用标准微任务语义，更适合需要跨浏览器和 Node 表达一致调度意图的代码。两者都不该承担无界批处理。

例子：递归调用 process.nextTick(loop) 会让已到期定时器迟迟不执行。正确做法是给单批工作设上限，处理一批后用 setImmediate 等方式主动让出事件循环，同时支持取消和总量限制。

面试表达：我不会把 nextTick 解释成“下一轮”。它发生在事件循环继续推进之前；优势是能尽快完成当前操作后的收尾，风险是递归追加会饿死 I/O。`,

	"d15-worker-boundary": `结论：Worker Threads 主要解决占用 JavaScript 线程的 CPU 密集计算，不是普通网络 I/O 的默认方案。Node 等待网络、文件或数据库结果时，本就可借助操作系统和 libuv 异步完成，不需要再占一个 Worker。

展开：Worker 能利用其他 CPU 核，但会带来线程启动、任务排队、消息序列化或共享内存同步成本。生产中应使用有上限的复用线程池，并给任务设置超时、取消与过载策略，而不是每个请求新建线程。

例子：大规模 JSON 计算、图片处理、压缩或加密可能值得移入 Worker；一次普通 HTTP 请求主要是在等待远端响应，放进 Worker 反而增加传输和调度开销。

面试表达：我的判断标准是事件循环是否被实际计算持续占满。先测事件循环延迟和任务耗时，再决定拆分、Worker 池或独立服务，不会把“异步”和“并行”混为一谈。`,

	"practice-d15-concurrency-pool": `结论：runPool 应创建不超过 limit 个领取循环，用共享游标取得下一项，把结果写回原索引。开始前校验 limit 为正整数，并明确采用 fail-fast，还是等待全部任务后返回逐项成功/失败结果。

展开：每个 worker 领取唯一索引后执行 tasks[index]，完成再领取下一项；输出数组按索引写入，所以完成顺序可以乱、返回顺序不乱。fail-fast 只能停止派发尚未开始的任务，已经在途的任务若要取消，任务接口还必须接收 AbortSignal。并发峰值测试应记录同时运行计数。

例子：五个网络任务、limit 为 2 时，任意时刻最多两个 Promise 在途；第二项即使最后完成，仍写到结果下标 1。若第三项失败，测试需要证明所选错误策略以及在途任务如何收尾。

面试表达：这个池提升的是等待型 I/O 的重叠度，CPU 密集 JavaScript 仍在同一事件循环线程串行争用。要利用多核，需要 Worker Threads 或进程，并同时考虑传输成本和池容量。`,

	"output-d15-node-service-boundary": `结论：边界图应从请求入口一直画到缓存、数据库和外部 API，并把“同步 CPU 工作”和“等待型 I/O”分别标出。重点不是画层数，而是明确每层责任、失败归属、资源上限及事件循环风险。

展开：入口/认证[待填]；参数校验[待填]；Controller 或路由仅做协议适配[待填]；业务编排[待填]；缓存/数据库/外部 API[待填]。每条外部调用补充超时[待填]、并发上限[待填]、取消[待填]、重试条件[待填]、降级[待填]和关联日志[待填]。CPU 密集步骤注明拆分、限流、Worker 池或独立服务方案[待填]。

例子：可选择自己的 BFF；若没有真实案例，明确写“练习场景”。沿一条核心请求标注依赖失败时返回什么、是否还能提供部分数据，以及哪段同步处理可能拉高事件循环延迟，数据均保留[待填]。

面试表达：我会先口述主链路，再沿失败链路倒查：谁超时、谁取消、谁降级、谁记录指标。最后说明一次最值得治理的事件循环风险[待填]，不把未经测量的性能收益说成事实。`,

	"d16-stream-backpressure": `结论：Writable.write 返回 false 不是写入失败，而是数据已被接收进缓冲区且缓冲达到 highWaterMark；生产者应暂停继续写，等待 drain 后恢复，并同时监听 error、close 等结束信号。

展开：highWaterMark 是触发背压的阈值，不是严格内存上限。若无视 false，生产速度长期高于消费速度，待写数据会持续堆积，引发更高内存、频繁 GC，极端时进程崩溃。pipe 和 pipeline 能协助传播背压，自定义流仍要遵守协议。

例子：快速读取文件并写入慢速网络连接时，每次 write 后检查返回值；为 false 就停止从上游领取数据，在 drain 后再继续，而不是用固定延时猜测下游何时恢复。

面试表达：我会明确说“false 表示该停，不表示该重写”。随后从生产速率、缓冲阈值、drain 恢复和错误清理四步解释背压闭环。`,

	"d16-buffer-boundary": `结论：readFile 必须让完整文件进入内存，解析和转换还可能产生额外副本；流式管道按块处理，并依靠背压限制在途数据，因此面对未知或很大的文件时内存峰值更可控。

展开：流式并不自动正确。Buffer 边界不等于字符、行或业务记录边界；UTF-8 多字节字符可能跨块，分隔符也可能被拆开。应使用 StringDecoder、成熟逐行解析器或显式保留尾部残片，同时处理错误、取消和部分输出。

例子：转换大型日志时，保留本块最后一段未闭合内容，与下一块拼接后再按换行解析。只有完整记录进入业务 Transform，成功后再原子替换目标文件。

面试表达：我会用“峰值内存可控、可提前产出、复杂度转移到边界处理”概括取舍，不会声称 data 事件天然对应一行。`,

	"d16-graceful-shutdown": `结论：收到 SIGTERM 后先让实例退出就绪状态并停止接收新流量，再在有限期限内等待在途请求、队列任务和流结束，随后刷新必要遥测、关闭数据库等资源；超时则强制终止并留下失败信号。

展开：退出流程要幂等，重复信号不能重复关闭或破坏状态。服务应停止监听新连接，为长连接设计通知或截止时间，拒绝启动新的后台工作，并让健康检查、负载均衡摘流和进程退出窗口相互配合。

例子：部署滚动更新时，先将 readiness 置为失败，再调用 server.close；同时启动总超时。所有连接和任务完成后按依赖顺序关闭资源，若超过期限则记录未完成数量后强制退出。

面试表达：回答顺序是“摘流—停止接单—排空—关资源—超时兜底”。我还会说明直接 process.exit 可能丢失尚未刷出的日志和缓冲数据。`,

	"practice-d16-stream-pipeline": `结论：使用 Promise 版 stream.pipeline 连接 Readable、逐行解析 Transform、业务 Transform 和 Writable，并把 AbortSignal 传给 pipeline。任一阶段报错或取消时，由 pipeline 销毁整条链路，调用方统一清理临时输出。

展开：createReadStream 不整文件加载；逐行 Transform 保存跨块残片，解码使用 StringDecoder 或可靠解析库；业务 Transform 通过 callback 或异步转换完成后再继续，让下游速度自然反向施加背压。输出先写临时文件，只有整条 pipeline 成功才重命名，失败时删除临时产物并保留原文件。

例子：测试至少包含超大单行、中文字符跨块、下游故意变慢、中途 Abort、解析器抛错和磁盘写失败。监控处理条数与内存峰值，但不要依赖一次机器测量承诺固定上限。

面试表达：我会从“分块边界、背压、取消、错误传播、结果原子性”五个方面讲实现。pipeline 的价值不只是语法短，而是把多段流的关闭与错误收敛到同一个 Promise。`,

	"output-d16-reliability-checklist": `结论：可靠性清单必须能在发布前被实际执行，而不是罗列“注意异常”。每项都要写适用边界、负责人、验证方式和失败标准，覆盖输入上限、流控、取消、退出与依赖故障。

展开：输入类型/文件上限[待填]；解析边界[待填]；highWaterMark 与背压验证[待填]；单请求及总任务超时[待填]；AbortSignal 传播[待填]；异常关闭与临时文件清理[待填]；连接/句柄释放[待填]；健康检查[待填]；优雅退出窗口[待填]；重试幂等和次数上限[待填]；日志、指标、告警[待填]。

例子：选择一个真实数据处理服务[待填]；无真实项目则标注练习。为“下游写入变慢”“数据库超时”“退出时仍有任务”分别写故障注入步骤[待填]、预期状态[待填]和恢复验证[待填]。

面试表达：我会挑最关键的三项讲证据：怎样证明没有无界缓冲[待填]、怎样证明取消会关闭全链路[待填]、怎样证明重试不会放大故障[待填]。`,

	"d17-nest-di": `结论：Provider token 表达消费者需要的能力，具体实现由模块组合层绑定。消费者不直接 new 实现，便可用 useClass、useValue、useFactory 或 useExisting 在不同环境替换配置、适配器和测试替身。

展开：这种分离让业务类关注契约，也让外部服务、缓存或存储实现的切换集中在模块边界；动态配置可由 factory 注入其他依赖。代价是绑定关系更间接，token 命名、导出范围和生命周期必须清晰，不能为每个类机械加一层抽象。

例子：订单服务依赖 PAYMENT_GATEWAY token，生产模块绑定真实支付适配器，测试模块绑定可控 fake；订单服务代码无需知道二者如何构造，也不会在测试中发出真实请求。

面试表达：我会把价值落到“组合层替换与可测试性”，再补充 request scope 会沿依赖链增加逐请求实例化成本，应只在确需请求上下文时使用。`,

	"d17-request-chain": `结论：Middleware 做框架入口级预处理；Guard 基于执行上下文决定是否放行；Interceptor 包裹 handler 做计时、日志、缓存或响应转换；Exception Filter 把未处理异常映射为稳定、安全的协议响应。

展开：典型顺序是 Middleware、Guard、Interceptor 前置、Pipe 参数解析校验、Controller/Provider、Interceptor 后置；异常若未在链内处理，再由 Filter 响应。逻辑应放在能获得所需上下文且职责最窄的位置，参数校验通常属于 Pipe，不属于权限 Guard。

例子：先在 Guard 验证身份与订单读取权限，Pipe 把 id 转为合法类型，Interceptor 记录耗时并包装成功结果，数据库异常最终由 Filter 映射为受控错误码和关联 ID。

面试表达：我会按一次请求的真实顺序讲，而不是并列背定义；每一层都回答“它能否阻断、是否包裹 handler、负责成功还是异常路径”。`,

	"d17-error-contract": `结论：统一 Filter 应统一错误契约，而不是把所有异常抹成 HTTP 200 或同一句文案。HTTP 状态表达协议语义，稳定业务码供客户端分支处理，安全消息面向用户，内部原因和堆栈只进入受控日志。

展开：全部返回 200 会误导缓存、网关、监控和重试策略；全部返回同一状态又会让客户端无法区分参数错误、无权限、冲突和服务暂时不可用。Filter 还应附关联 ID，但要避免输出 SQL、密钥、堆栈或敏感请求数据。

例子：非法参数返回 400 和稳定参数错误码；资源不存在返回 404；瞬时依赖不可用可返回 503，并由调用方按幂等性决定是否退避重试。日志用同一关联 ID 保存内部 cause。

面试表达：我会分成“协议层、业务层、诊断层”三层说明，并强调只有明确可恢复、且操作幂等的错误才适合自动重试。`,

	"practice-d17-nest-request-chain": `结论：订单读取接口应把认证和资源授权放 Guard，DTO 解析与参数约束放 Pipe，围绕处理器的计时和安全响应转换放 Interceptor，异常到 HTTP 状态与业务码的映射放 Exception Filter。

展开：请求先经过认证 Guard，得到可信主体后再做订单级授权；Pipe 把路径参数转换为目标类型并拒绝非法输入。Interceptor 在调用前记录时间和关联信息，在成功后只输出允许字段；Filter 处理未捕获异常，对外隐藏数据库和堆栈细节。Controller 只做协议编排，业务读取留在 Provider。

例子：测试无凭证、无权限、非法 id 时 handler 均不执行；成功时响应字段符合契约；资源不存在、数据库超时和未知异常分别映射为预期状态，日志可用 requestId 找到内部原因。

面试表达：我会沿真实执行顺序解释每层，而不是说“都能做所以随便放”。判断依据是是否负责放行、参数、调用包裹或异常映射，以及拒绝路径能否被独立测试。`,

	"output-d17-nest-lifecycle": `结论：两分钟讲稿要用一条请求串起所有横切层，并始终区分成功链路与异常链路。重点是每层只有一个主要职责，错误对外形成稳定契约、对内保留可关联诊断信息。

展开：请求进入 Middleware，完成通用上下文/关联 ID[待填]；Guard 做身份与权限判断[待填]；Pipe 解析并校验参数[待填]；Interceptor 前置计时或日志[待填]；Controller 把协议请求交给 Provider[待填]；成功结果经过 Interceptor 后置转换[待填]；未处理异常交给 Filter 映射状态、业务码和安全消息[待填]。

例子：选一个真实接口[待填]，分别准备成功、无权限、参数错误和依赖超时四条路径；没有真实项目就标“练习接口”。补充 requestId 如何贯穿日志[待填]，不要填写未经验证的耗时数字。

面试表达：口述顺序固定为“入口—放行—参数—包裹—业务—返回或异常”。结尾用一句取舍说明：横切层不是越多越好，只有需要对应上下文且可独立测试的逻辑才进入该层。`,

	"d18-index-selectivity": `结论：数据库优化器按预计成本选择计划，不会因为存在索引就强制使用。条件命中比例很高、表很小，或索引随机访问加大量回表比连续读取更贵时，顺序扫描可能是更优选择。

展开：选择还受统计信息、数据分布、相关性、返回列、缓存状态和成本参数影响。索引不是免费加速器，会占存储并增加插入、更新和维护开销；判断必须落到代表性数据下的 EXPLAIN ANALYZE 和真实查询指标。

例子：状态列只有“启用/停用”，若启用占绝大多数，查询全部启用行即使有单列索引也可能顺序扫描；而查极少数停用行时，同一索引更可能有价值。

面试表达：我会先问命中多少行、表多大、是否需要回表，再看估计与实际行数。未走索引不等于数据库出错，计划是否合理要用成本和实测解释。`,

	"d18-multicolumn-index": `结论：多列 B-tree 的列顺序应由真实过滤、连接和排序模式决定。常见有效形态是前导列承担等值约束，随后列承担范围或排序，但它是基于访问路径的设计原则，不是只按单列选择性排序的口诀。

展开：要检查查询是否总带前导列、范围条件从哪一列开始、ORDER BY 能否复用索引顺序、是否需要覆盖列，以及数据库版本的具体能力。某些数据库可组合单列索引或使用跳跃扫描，但不能据此忽略代表性数据验证。

例子：查询总是按 tenant_id 等值过滤，再按 created_at 做时间范围并倒序分页，(tenant_id, created_at) 通常比反向顺序更贴合路径；若还常单独按 created_at 查全局数据，需要另行评估。

面试表达：我会先列出高频 SQL 形状，再解释候选索引服务了哪些谓词和排序，同时说明写放大、存储和冗余索引的代价，最后用 EXPLAIN 验证。`,

	"d18-explain-analyze": `结论：估计行数严重偏差会让优化器在执行前选错连接顺序、连接算法和扫描方式，并把误差层层放大；单个节点慢只描述一次结果，估计偏差则暴露计划选择依据本身可能失真。

展开：阅读时比较 estimated rows 与 actual rows，并结合 loops 理解节点被执行多少次，再看实际耗时和 buffers。偏差可能来自统计信息过旧、列相关性未表达、数据倾斜、谓词写法或参数分布，不能只靠“加索引”处理。

例子：优化器估计内表返回 10 行，实际每次返回 10 万行，可能错误选择嵌套循环；应先定位估计来源，更新或扩展统计、调整谓词，再重新比较计划，而不是只盯总耗时。

面试表达：我的阅读顺序是“最终结果—估计/实际行数—loops—耗时和缓冲—偏差来源”。执行写语句的 ANALYZE 会真的执行，应放在可回滚或安全环境。`,

	"practice-d18-explain": `结论：比较三份计划时，先用实际总耗时、返回行数、loops 和 buffers 建立事实，再判断扫描方式是否与命中比例相符，最后比较 estimated rows 与 actual rows。不能把“出现 Seq Scan”直接判为问题。

展开：顺序扫描若面对小表或高命中比例可能合理；索引扫描要看索引过滤后返回多少、回表和随机访问成本；估计严重偏差的计划先追查统计信息、数据倾斜、列相关性和谓词。所有建议都要说明验证 SQL、代表性数据、读收益假设，以及新增索引的写入和存储代价。

例子：为每份计划写三栏：观察事实[待填]、判断与依据[待填]、下一步实验[待填]。对嵌套循环尤其结合 loops，避免把节点显示的单次平均值直接当作全局总量。

面试表达：我会明确区分“计划看起来不同”和“已证实性能问题”。先修估计偏差再比较候选方案，用同一数据规模和参数复测，并预先写撤销条件，不承诺未经验证的收益。`,

	"output-d18-index-proposal": `结论：索引提案应从一条具体查询及其证据出发，把收益写成待验证假设，同时完整列出写放大、存储、维护和冗余风险。提案不是“给字段加索引”，而是可复现的实验与上线决策。

展开：查询文本与调用频率[待填]；数据规模和分布[待填]；当前 EXPLAIN ANALYZE、loops、buffers[待填]；瓶颈证据[待填]；候选索引及列顺序理由[待填]；对过滤/排序/连接的作用[待填]；写入和存储成本[待填]；与现有索引重叠[待填]；代表性数据实验[待填]。

例子：可用真实慢查询或明确标注的练习 SQL。记录修改前后相同参数、相同数据量下的计划和延迟[待填]；上线采用范围[待填]，监控读延迟、写延迟、索引大小和命中[待填]，撤销阈值[待填]。

面试表达：我会先讲查询形状，再讲为什么这个列序匹配访问路径，接着主动说明写成本。结尾强调收益在代表性验证完成前只是推测，避免把一次本机结果包装成生产结论。`,

	"d19-isolation-anomalies": `结论：事务定义一组操作的原子边界，隔离级别则约束并发事务彼此能观察到什么，以及哪些并发结果必须等待、冲突或回滚。它解决的是并发可见性与业务不变量，不是简单的“是否开启事务”。

展开：不同级别允许或阻止脏读、不可重复读、幻读和更广义的序列化异常。更强隔离通常带来更多冲突、等待或重试；选择时应先写出库存非负、余额守恒等不变量，再决定条件更新、显式锁或 Serializable。

例子：两个事务都先读到最后一名额，再各自写入。即使每个事务内部原子，弱隔离下组合结果仍可能违背名额上限；串行化或能原子检查条件的写操作可让一个失败并重试。

面试表达：我会先区分原子性和隔离性，再用一个不变量推演异常，最后说明“级别越高越好”不成立，方案还要定义冲突失败如何恢复。`,

	"d19-mvcc-snapshot": `结论：MVCC 为同一逻辑行保留可见性不同的版本，查询按自己的快照判断应读哪个版本，因此普通读通常无需等待另一个事务正在更新的版本；但它绝不等于数据库没有锁。

展开：写写冲突、SELECT FOR UPDATE、唯一约束检查、DDL 和部分维护操作仍会锁定或等待。旧版本只有在不再可能被任何快照看到后才能回收；长事务长期持有旧快照，会推迟清理并导致表膨胀等问题。

例子：事务 A 更新一行但尚未提交，事务 B 的普通查询可按快照读取旧已提交版本，而不是读取未提交值；若 B 也要更新同一行，则仍可能等待 A 或发生冲突。

面试表达：我会用“多版本 + 快照可见性”解释读写不互堵，再主动列出写写、显式锁和 DDL 反例，避免把 MVCC 说成复制整表或完全无锁。`,

	"d19-lock-deadlock": `结论：降低死锁概率要让事务按一致顺序获取资源、缩短事务时间、减少不必要锁范围，并避免在持锁期间等待外部调用。但复杂并发下仍无法假定绝无死锁，应用必须把死锁视为可处理的事务失败。

展开：数据库检测到等待环后通常中止其中一个事务。重试必须从事务开始重新读取数据，因为原快照和判断已经失效；只对可识别的瞬时冲突做有次数上限、带退避的重试，且整个业务操作具备幂等边界。

例子：转账同时锁两账户时统一按账户 ID 从小到大加锁，可减少 A 等 B、B 等 A 的环路；若仍收到死锁错误，回滚全部操作，短暂退避后重新开启事务，而非继续使用旧值。

面试表达：我会从“预防、检测、恢复”三层回答。无限重试不是恢复策略，它可能放大竞争；还应监控死锁率、锁等待和重试耗尽。`,

	"practice-d19-transaction-timeline": `结论：先逐行画出两个事务的 BEGIN、SELECT、UPDATE、COMMIT，并在每一步注明读到的版本、持有的锁和可能的等待。然后围绕“库存不得为负或超卖”这个不变量比较方案，而不是只背异常名称。

展开：在 Read Committed 下，每条语句取得新快照；若应用先读库存，再按旧值写绝对结果，可能发生覆盖或错误决策。单条条件 UPDATE 可把检查与扣减合并。SELECT FOR UPDATE 让第二个事务等待并在获得锁后重新判断。Serializable 可检测不可串行化组合，中止一个事务，应用从头有限重试。

例子：初始库存与两笔扣减量均写为练习参数[待填]。时间线上分别标注 T1/T2 的读值、更新条件、阻塞点和最终结果；若模拟失败，明确返回缺货还是重试，不虚构生产数据。

面试表达：我会先给出弱方案为何破坏不变量，再比较条件更新、显式锁和 Serializable 的吞吐、等待与开发复杂度。重试必须重新读数据，并依赖幂等键防止外层请求重复副作用。`,

	"output-d19-transaction-adr": `结论：事务 ADR 的起点是可验证业务不变量和并发操作，不是先选隔离级别。方案必须同时定义事务边界、冲突时的失败语义、幂等与有限重试，以及上线后如何观察锁等待和冲突。

展开：场景与事实源[待填]；业务不变量[待填]；会并发发生的读写[待填]；可接受与不可接受异常[待填]；候选方案：条件更新/显式锁/Serializable/乐观版本[待填]；选定方案及原因[待填]；事务内禁止的外部调用[待填]；失败码[待填]；幂等键和重试上限[待填]。

例子：可选择库存、余额或状态流转；若非真实经历必须写“练习场景”。用两事务时间线验证方案[待填]，列出锁等待、序列化失败、重试耗尽和人工补偿测试[待填]，不填写臆测成功率。

面试表达：我会先说不变量，再用竞态时间线证明为何需要控制，最后比较所选方案与替代方案的等待、冲突和维护成本。监控项包括事务时长、锁等待、死锁或序列化失败率[待填]。`,

	"d20-cache-failure-modes": `结论：穿透是查询不存在的数据反复越过缓存；击穿是单个热点键失效后大量请求同时回源；雪崩是大量键集中失效或缓存服务整体不可用。三者的影响范围不同，不能只靠同一把锁治理。

展开：穿透可做输入校验、短期负缓存或过滤器，并处理真实数据新建后的失效；击穿可用同键 single-flight、逻辑过期、预热；雪崩要给 TTL 加抖动，并准备限流、降级、隔离和数据源容量保护。

例子：不存在的商品 ID 持续请求属于穿透；一个爆款详情键到期瞬间并发回源属于击穿；一批键同一秒过期或 Redis 故障使全站回源属于雪崩。

面试表达：我会按“不存在的键、单热点键、广泛失效”先定义，再逐一匹配措施，并强调缓存故障时数据库限流比盲目降级回源更重要。`,

	"d20-expiration-eviction": `结论：过期由单个键的 TTL 表达数据何时失效；淘汰是 Redis 达到 maxmemory 后按配置策略释放键以维持内存边界。前者服务数据新鲜度，后者服务容量控制，是两个独立机制。

展开：设置 TTL 不代表到期毫秒立刻物理删除，键可能在访问时惰性删除或由后台周期清理。有 TTL 的键也可能提前被淘汰；在 allkeys 策略下，无 TTL 键同样可能被淘汰。应用不能把缓存存在性当作事实源。

例子：商品详情 TTL 为十分钟，表达十分钟后需重新验证；若内存提前达到上限，它可能在十分钟前就被淘汰。反过来，到期后若尚未被访问，内存释放也不一定精确同步。

面试表达：我会用“TTL 管新鲜度，maxmemory policy 管生存空间”概括，再说明随机抖动可避免大量 TTL 同时到期，却不能替代故障降级。`,

	"d20-distributed-lock": `结论：Redis 租约锁至少要用唯一令牌标识持有者，以 SET key token NX PX ttl 原子建立锁，并用 Lua 等原子比较令牌后删除。否则旧任务超时后可能误删新持有者刚获得的锁。

展开：TTL 防止持有者崩溃造成永久锁，但暂停、网络分区或任务超时仍可能让旧持有者在租约失效后继续执行。续租也不能消除所有时序风险；关键资源需使用单调递增 fencing token，或在数据库等下游施加版本/唯一约束。

例子：客户端 A 卡顿至租约过期，B 获得新锁。A 恢复后若直接 DEL，会删掉 B 的锁；compare-delete 会因令牌不同拒绝删除。即便不误删，A 仍可能发出过期写入，因此下游还要拒绝旧 fencing token。

面试表达：我会分“获得、释放、过期持有者”三层回答，并明确 Redis 锁只协调客户端，不自动保证最终资源安全。`,

	"practice-d20-cache-aside": `结论：读取流程是先查 Redis，命中数据直接返回，命中负值返回不存在；未命中时按 key 用 single-flight 合并本进程并发回源，数据库查询设置超时。成功结果写带随机抖动的 TTL，空结果只写较短负缓存。

展开：Redis 读失败不能无限并发打数据库，应进入受限降级路径：全局和按依赖限流、并发上限、快速失败或返回可接受的旧数据[待按业务决定]。数据库不是永远可重试，超时后的重试需满足幂等、总期限和退避；写缓存失败只影响后续命中，不应把缓存升级为事实源。

例子：伪代码需区分 CACHE_MISS、NEGATIVE 和真实值；用 finally 清理 single-flight 条目。测试并发一百个同键未命中只触发受控回源，还要覆盖 Redis 故障、数据库超时、真实数据创建后的负缓存失效和 TTL 抖动范围。

面试表达：我会说明 single-flight 主要治理单热点击穿，负缓存治理穿透，TTL 抖动降低集中失效；缓存整体故障仍靠限流与降级保护数据库，三者不能混成一个方案。`,

	"output-d20-cache-design": `结论：缓存设计必须先写清事实源与可接受一致性，再定义 key、TTL、更新和故障行为。Redis 只是加速层；当它不可用时，系统应保护数据库并做明确降级，而不是无上限直连回源。

展开：数据源[待填]；读写比例与一致性要求[待填]；key/version/value 大小[待填]；TTL 和随机抖动[待填]；cache-aside 或其他更新策略[待填]；写后失效顺序[待填]；空值与失效机制[待填]；热点 single-flight[待填]；Redis 超时、熔断、降级[待填]；数据库并发上限[待填]。

例子：选一个真实读多写少接口[待填]，无真实数据则标练习。推演穿透、热点键过期、批量过期、Redis 全部不可用和数据刚更新五条时间线；为每条写预期行为、恢复步骤和演练方式[待填]。

面试表达：我会先说缓存不是什么——不是事实源，也不是可用性保证。再讲命中率、p95/p99 延迟、回源率、错误率、数据库连接占用和热 key[待填]，最后给出撤销或旁路条件[待填]。`,

	"d21-requirements-first": `结论：组件选择取决于要解决的用户路径、规模和非功能约束。先澄清核心用例、读写比例、峰值、延迟、可用性、一致性、数据保留、合规和成本，才能判断缓存、队列、分片等是否必要。

展开：估算不要求绝对准确，价值在于把假设写清并让架构能随量级变化。每个组件都应回扣一个已确认约束；如果没有对应问题，就不该为了“完整”堆进图里。还要明确不做什么和允许怎样失败。

例子：通知系统若只需最终送达与单用户局部顺序，和要求全局严格顺序的方案完全不同；若断线后允许补拉，就不必让 WebSocket 成为唯一可靠交付通道。

面试表达：我开场会问“谁在什么场景做什么、峰值多少、允许多慢和怎样失败”，给出量级假设后再画主链路，并在每次引入组件时说出它解决的约束。`,

	"d21-idempotency-retry": `结论：超时只表示调用方没拿到确定结果，不代表服务端没执行。重试写操作必须用幂等键或业务唯一约束把重复请求收敛到同一结果，并配合指数退避、随机抖动、单次及总超时和次数上限。

展开：幂等记录要绑定请求主体与关键参数，重复 key 但参数不同应拒绝；保留时间覆盖客户端可能重试的窗口。只重试明确瞬时错误，永久参数错误不重试。多层调用还需统一重试预算，避免每层各重试造成指数放大。

例子：创建订单请求携带 idempotencyKey；首次结果即使响应丢失，第二次请求也返回同一订单，而不是再创建一笔。遇到 503 时在总期限内退避重试，遇到 400 立即失败。

面试表达：我会从“结果未知”解释重复副作用，再讲幂等键、唯一约束与结果缓存，最后补齐退避、抖动和预算。禁用按钮只能改善交互，不构成服务端幂等。`,

	"d21-observability": `结论：指标负责用聚合时间序列发现趋势和告警，日志记录带上下文的离散事件，追踪用 trace/span 串起一次请求跨服务的因果路径。三者解决不同问题，应通过统一关联标识和版本信息互相跳转。

展开：设计应从 SLI/SLO 和故障问题出发，而非先收集一切。指标标签要控制基数，用户 ID、请求 ID 不适合作为指标标签；日志需结构化并脱敏；追踪可采样，但错误和慢请求应采用合适保留策略。

例子：错误率指标触发告警后，按版本和服务定位异常时段；从一个 exemplar 或 traceId 打开慢请求追踪，找到下游 span，再查询同一关联 ID 的受控错误日志获取细节。

面试表达：我会用“指标告诉我是否出事，追踪告诉我卡在哪，日志告诉我具体发生什么”概括，再说明大量日志不等于可观测，关键是能回答预先未知的问题。`,

	"practice-d21-notification-design": `结论：先确认站内信是否为事实记录、WebSocket 只做实时提示，以及送达目标和单用户顺序范围。写请求携带幂等键，事务内落消息与 outbox；异步分发按用户或会话分区，客户端以消息 ID/序列号去重，断线后按最后游标补拉。

展开：主链路是 API 校验→数据库事务→outbox/MQ→推送网关→在线连接；未读数采用可校正模型[待按一致性要求选择]。至少一次投递允许重复，因此消费者幂等。慢消费者设置队列上限并断开或降级轮询；热点用户、重连风暴和 MQ 积压都需限流与背压。

例子：客户端收到序号 101 后断线，重连携带 lastSeen=101，从持久存储补拉 102 之后消息；重复的 103 由 messageId 去重，发现序号间隙则再次补拉。WebSocket 推送失败不应丢失站内信。

面试表达：我会依次讲需求与规模[待填]、写入事实链路、实时推送、断线恢复、重复乱序、降级与监控。关键指标包括 outbox/MQ 积压、端到端送达延迟、重连率、推送失败率和补拉成功率。`,

	"output-d21-system-design-talk": `结论：12 分钟陈述应以约束驱动架构，先给主链路，再讲失败恢复，最后才谈扩展。每一段有时间预算，确保消息持久化、实时推送和断线补偿三条关键语义在超时前讲完整。

展开：第 1 分钟：用户路径、规模、送达与顺序目标[待填]；第 2–3 分钟：API、消息/游标/幂等模型[待填]；第 4–7 分钟：事务落库、outbox/MQ、分区、WebSocket 主链路[待填]；第 8–9 分钟：重复、乱序、断线补拉、慢消费者和降级[待填]；第 10 分钟：指标告警[待填]；第 11–12 分钟：取舍与演进[待填]。

例子：画一条从“创建通知”到“用户确认游标”的时序图[待填]。准备三条深入追问：热点用户或分区倾斜怎样处理[待填]；客户端长时间断线怎样补偿[待填]；WebSocket 或 MQ 故障怎样降级并恢复[待填]。

面试表达：开场先声明假设，组件出现时立即说明它解决什么。结尾复述一致性边界：站内信事实源[待填]、实时通道允许降级[待填]、顺序范围[待填]，并指出当前规模下暂不引入的复杂度[待填]。`,
};
